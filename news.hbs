{{!< default}}

<div class="page-width">
    <h1>The latest news about Wormwood Saga...</h1>
    {{#foreach posts}}
        <a href="{{url}}">
            {{ title }}
        </a>
    {{/foreach}}
</div>