{{!< default}}

<div>
    <header class="header">
            <div class="header__nav">
                {{navigation}}
            </div>
        </header>
    <div class="container">
        <div class="row">
            <h1 class="title">{{meta_title}}</h1>
        </div>
        
        <div class="row justify-content-center">
            <div>  {{! see .img-fluid in bootstrap css file to style logo line 536}}
                <img src="{{@site.logo}}" class="img-fluid">    
            </div>          
        </div> 
        <div class="row justify-content-end">
            <div class="col-sm-4">
                <p class="author">written by Isaac Tait</p>
            </div>
        </div>
    </div>
</div>